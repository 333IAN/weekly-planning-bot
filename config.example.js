/**
 * Configuration Template for Weekly Planning Bot
 * 
 * INSTRUCTIONS:
 * 1. Copy this file and rename it to: config.js
 * 2. Fill in your actual values below
 * 3. Do NOT commit config.js to Git
 * 4. Keep config.example.js in Git as reference
 */

const CONFIG = {
  // Your Google Apps Script Web App deployment URL
  // Find this in: Google Apps Script → Deploy → Manage Deployments
  GOOGLE_SCRIPT_URL: "https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec",

  // Your email address (where reminders will be sent)
  RECIPIENT_EMAIL: "your-email@gmail.com",

  // URL to your task management dashboard (Notion, Trello, GitHub Projects, etc.)
  // This link will appear in your email reminders and web interface
  TASKS_URL: "https://your-tasks-dashboard-url/"
};
